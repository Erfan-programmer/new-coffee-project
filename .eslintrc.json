
{
  "overrides": [
    {
      "files": ["*.js", "*.ts", "*.tsx"], // specify the extensions here
      "rules": {
        "no-unused-vars": "off",
        "no-console": "off",
        "react/react-in-jsx-scope": "off"
        // Add more rules as needed, or use a wildcard to disable all rules
      }
    }
  ]
}
